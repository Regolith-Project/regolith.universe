[pytest]
markers =
    render: launches a real Gazebo GUI and grades a screenshot; needs a GPU, a display
        and about a minute per launch. These are the only tests that can see a
        RENDERING fault - deselecting them (-m "not render") leaves the floating-rocks
        regression uncovered.
